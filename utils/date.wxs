var trim = function (date) {
  if (date)
    return date.split(' ')[0]
  else
    return date
}

var parse = function (date) {
  while (date.indexOf('-') != -1) {
    date = date.replace('-', '/')
  }
  return date
}
module.exports = {
  trim: trim,
  parse: parse,
}

