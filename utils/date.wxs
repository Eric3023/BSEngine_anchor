var trim = function (date) {
  return date.split(' ')[0]
}

var parse = function (date) {
  while(date.indexOf('-') != -1){
    date = date.replace('-', '/')
  }
  return date
}
module.exports = {
  trim: trim,
  parse: parse,
}

