<view class="container">
	<view class="item" wx:for="{{plateforms}}" wx:key="index" data-item="{{item}}" catchtap="onClickItem">
		<image class="icon" src="{{item.icon}}"></image>
		<text class="name">{{item.name}}</text>	
	</view>
</view>